{
  "name": "newsletter-subscription",
  "version": "1.0.0",
  "description": "Newsletter subscription Lambda for Hybetech - stores to SharePoint List",
  "type": "commonjs",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../function.zip ."
  },
  "dependencies": {
    "@azure/msal-node": "^2.6.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "archiver": "^7.0.1",
    "typescript": "^5.3.0"
  }
}
